# Contributing to NFTables Manager

1. Fork the repo
2. Create a branch: git checkout -b feature/xyz
3. Commit changes: git commit -m "Add feature"
4. Push: git push origin feature/xyz
5. Open Pull Request

Follow GPLv3 and update README if needed.
