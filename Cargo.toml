[package]
name = "nftables-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = { package = "gtk4", version = "0.7" }
glib = "0.17"
libadwaita = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
